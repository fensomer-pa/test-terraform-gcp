# .github/workflows/deploy.yml
name: Deploy

on:
  push:
    branches:
      - "feature/**"

jobs:
  plan-dev:
    environment: dev
    uses: fensomer-pa/test-terraform-gcp/.github/workflows/tf_plan.yml@v1.0.0
    secrets: inherit
    permissions:
      contents: read
      id-token: write

